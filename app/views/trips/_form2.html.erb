<%= form_for(@new_crew) do |f| %>
  <% if @new_crew.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_crew.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @new_crew.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :bio %><br>
    <%= f.text_field :bio %>
  </div>
  <div class="field">
    <%= f.label :notifications %><br>
    <%= f.checkbox :notifications %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
